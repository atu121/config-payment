configuration:  
  projectName: hystrix-service

server:
  port: 8091

eureka:  
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8090/eureka/
    healthcheck:
      enabled: true
    lease:
      duration: 5
